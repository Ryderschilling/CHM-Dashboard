DATABASE_URL="postgresql://user:password@host/neondb?sslmode=require"
DIRECT_URL="postgresql://user:password@host/neondb?sslmode=require"
DASHBOARD_PASSWORD="pick-a-strong-password"
SQUARE_ACCESS_TOKEN=""
SQUARE_LOCATION_ID=""

# Read-only iCal feed. Still used for the Coming Up strip on the dashboard.
GOOGLE_CALENDAR_ICS_URLS=""

# Two-way Google Calendar sync (Jobs page).
# Google Cloud Console -> APIs & Services -> Credentials -> OAuth client ID -> Web application.
# Authorized redirect URIs must include BOTH:
#   http://localhost:3005/api/google/callback
#   https://chm-dashboard.vercel.app/api/google/callback
# The refresh token is stored in the database by the Connect button, not here.
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
